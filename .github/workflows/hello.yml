name: My First GitHub Action

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello
        run: echo "Hello from GitHub Actions"

      - name: Check OS
        run: uname -a

      - name: Check Java
        run: java -version
